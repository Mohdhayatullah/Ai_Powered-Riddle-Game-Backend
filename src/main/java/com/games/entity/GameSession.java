package com.games.entity;import jakarta.persistence.*;import lombok.*;import java.time.LocalDateTime;@Entity@Table(name = "game_sessions")@Getter@Setter@NoArgsConstructor@AllArgsConstructor@Builderpublic class GameSession {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    @ManyToOne(fetch = FetchType.LAZY)    @JoinColumn(name = "user_id", nullable = false)    private User user;    @ManyToOne(fetch = FetchType.LAZY)    @JoinColumn(name = "riddle_id", nullable = false)    private Riddle riddle;    @Column(name = "hint_used")    @Builder.Default    private Boolean hintUsed = false;    @Column(name = "solved")    @Builder.Default    private Boolean solved = false;    @Column(name = "attempts")    @Builder.Default    private Integer attempts = 0;    @Column(name = "score_awarded")    @Builder.Default    private Integer scoreAwarded = 0;    @Column(name = "started_at")    @Builder.Default    private LocalDateTime startedAt = LocalDateTime.now();    @Column(name = "solved_at")    private LocalDateTime solvedAt;    // Chat memory for this session (stored as JSON string)    @Column(name = "chat_memory", columnDefinition = "TEXT")    @Builder.Default    private String chatMemory = "[]";}