package com.games.repository;import com.games.entity.GameSession;import com.games.entity.Riddle;import com.games.entity.User;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.stereotype.Repository;import java.util.Optional;@Repositorypublic interface GameSessionRepository extends JpaRepository<GameSession, Long> {    // Find active (unsolved) session for a user + riddle    Optional<GameSession> findByUserAndRiddle(User user, Riddle riddle);    // Find any session (solved or not)    Optional<GameSession> findByUserAndRiddleId(User user, Long riddleId);}