package com.games.service;import com.games.dto.LeaderboardEntry;import com.games.dto.MyStatsResponse;import com.games.entity.User;import com.games.repository.UserRepository;import lombok.RequiredArgsConstructor;import org.springframework.stereotype.Service;import java.util.ArrayList;import java.util.List;@Service@RequiredArgsConstructorpublic class LeaderboardService {    private final UserRepository userRepository;    // ── Top 10 Leaderboard ────────────────────────────────────────────────────    public List<LeaderboardEntry> getTop10() {        List<User> users = userRepository.findTop10ByOrderByTotalScoreDesc();        List<LeaderboardEntry> board = new ArrayList<>();        for (int i = 0; i < users.size(); i++) {            User u = users.get(i);            board.add(LeaderboardEntry.builder()                    .rank(i + 1)                    .username(u.getDisplayName())                    .totalScore(u.getTotalScore())                    .correctAnswers(u.getCorrectAnswers())                    .bestStreak(u.getBestStreak())                    .build());        }        return board;    }    // ── My Stats + Streak ─────────────────────────────────────────────────────    public MyStatsResponse getMyStats(User user) {        double accuracy = user.getTotalAttempts() == 0                ? 0.0                : Math.round((user.getCorrectAnswers() * 100.0 / user.getTotalAttempts()) * 10.0) / 10.0;        return MyStatsResponse.builder()                .username(user.getDisplayName())                .email(user.getEmail())                .totalScore(user.getTotalScore())                .correctAnswers(user.getCorrectAnswers())                .totalAttempts(user.getTotalAttempts())                .currentStreak(user.getCurrentStreak())                .bestStreak(user.getBestStreak())                .accuracy(accuracy)                .lastPlayedAt(user.getLastPlayedAt())                .memberSince(user.getCreatedAt())                .build();    }}