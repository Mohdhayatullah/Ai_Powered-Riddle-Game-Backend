# Use Java runtimeFROM openjdk:21-jdk-slim# Set working directoryWORKDIR /app# Copy jar file (after build)COPY target/riddle-0.0.1-SNAPSHOT.jar app.jar# Expose port (Spring Boot default)EXPOSE 8080# Run the appENTRYPOINT ["java","-jar","app.jar"]